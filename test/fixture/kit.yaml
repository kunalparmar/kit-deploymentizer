version: '2'
load:
  path: /test/fixtures
  # All paths defined here are relative to the load.path
  images:
    path: ./manifests/images
    property: image
  type:
    path: ./type
  cluster:
    path: ./clusters
  resources:
    path: ./resources
output:
  path: /generated
plugin:
  path: /src/plugin/file-config
  options:
    configPath: "/test/fixture/config"

#plugin:
#  path: /src/plugin/env-api
#  options:
#    apiUrl: "http://somehost/v1/api"
#    apiToken: "s0me-t0ken-g0es-here"
